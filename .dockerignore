# Python
__pycache__
*.pyc
*.pyo
*.pyd
*.egg-info
.pytest_cache
.cache
.venv
venv
env

# Frontend (built separately or served elsewhere)
frontend

# Tests + tooling (not needed in image)
tests
.ruff_cache
.mypy_cache

# Local + secrets
.env
.env.*
!.env.example
*.local

# VCS + editors
.git
.gitignore
.github
.idea
.vscode
.DS_Store

# Build artifacts
dist
build
# Ignore compiled contract artifacts EXCEPT the ABI, which the on-chain logger
# loads at runtime (app/services/on_chain_logger.py). The .bin is deploy-only.
contracts/out/*
!contracts/out/LiquidationCascadeLogger.abi.json

# Docs / assets not needed at runtime
*.docx
*.png
README.md
